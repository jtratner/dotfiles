export EDITOR=`which vim`

#thank you to Doug Hellman for being incredibly excellent and helping with settings for pip
# tell pip to use virtualenv_base as workon home
export PIP_VIRTUALENV_BASE=$WORKON_HOME

# tell pip to respect virtual_env
export PIP_RESPECT_VIRTUALENV=true
